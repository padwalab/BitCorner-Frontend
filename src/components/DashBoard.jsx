import React, { Component } from "react";
import { Container } from "react-bootstrap";

class DashBoard extends Component {
  state = {};
  render() {
    return <Container fluid> This will the the dashboard</Container>;
  }
}

export default DashBoard;
